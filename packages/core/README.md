# @chayprabs/duckdb-file-sql

Browser-first DuckDB SQL utilities extracted from FileSQL.

This package exposes the shared contracts, share-link helpers, read-only SQL safety helpers, and the lazy browser session loader used by the FileSQL playground.

For the full product, screenshots, and worker deployment path, see the root repository README.
